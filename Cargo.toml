[package]
name = "json_lang"
version = "0.1.0"
edition = "2021"

[dependencies]
serde={version = "1.0.217",features = ["derive"]}
serde_json={version = "1.0.138",features = ["default"]}
indexmap = {version = "2.7.1",features = ["serde"]}